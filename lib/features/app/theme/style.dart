import 'package:flutter/material.dart';

// Define your colors with blueish tones
const backgroundColor =
    Color.fromRGBO(20, 24, 36, 1); // Darker blueish background
const textColor = Color.fromRGBO(225, 230, 237, 1); // Light blueish text
const appBarColor = Color.fromRGBO(32, 40, 89, 1); // Blueish app bar
const bottomAttachContainerColor =
    Color.fromRGBO(25, 30, 55, 1); // Dark blueish bottom attach container
const webAppBarColor = Color.fromRGBO(21, 33, 50, 1); // Blueish web app bar
const messageColor = Color.fromRGBO(61, 88, 114, 1); // Blueish message
const senderMessageColor =
    Color.fromRGBO(28, 50, 79, 1); // Dark blueish sender message
const tabColor = Color.fromRGBO(86, 134, 180, 1); // Light blueish tab
const searchBarColor = Color.fromRGBO(24, 44, 70, 1); // Blueish search bar
const dividerColor = Color.fromRGBO(19, 37, 60, 1); // Dark blueish divider
const chatBarMessage =
    Color.fromRGBO(75, 120, 160, 1); // Blueish chat bar message
const mobileChatBoxColor =
    Color.fromRGBO(30, 52, 72, 1); // Dark blueish mobile chat box
const lightGreyColor = Color.fromRGBO(170, 200, 230, 1); // Light blueish grey
const greyColor = Color.fromRGBO(100, 120, 140, 1); // Blueish grey
const blackColor = Colors.black;
const whiteColor = Colors.white;//whiteColor

